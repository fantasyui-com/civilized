module.exports = function(){

  this.register( 'cross-platform', `App store should work on Windows, Linux, and Mac.` );
  this.register( 'categories', `App store should have a homepage, categories, trending and installed.` );
  this.register( 'feed-management', `User should be able to add new feeds to the app store.` );

}
